import { ItemsPricePipe } from './items-price.pipe';

describe('ItemsPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsPricePipe();
    expect(pipe).toBeTruthy();
  });
});
