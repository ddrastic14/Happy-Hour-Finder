import { HhEndFilterPipe } from './hh-end-filter.pipe';

describe('HhEndFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HhEndFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
