import { HhStartFilterPipe } from './hh-start-filter.pipe';

describe('HhStartFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HhStartFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
